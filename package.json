{
  "name": "backend-boilerplate",
  "version": "1.0.0",
  "scripts": {
    "build": "dotnet build app/Amach.sln",
    "start": "cd app/Amach.API && dotnet run",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^9.7.0"
  }
}
